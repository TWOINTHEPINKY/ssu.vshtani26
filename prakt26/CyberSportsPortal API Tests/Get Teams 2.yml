info:
  name: Get Teams 2
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/Teams/1"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        const response = res.body;
        test('Проверка: наличие полей id, name и players.', function() {
          expect(response).to.be.an('object');
          expect(response).to.have.property('id');
          expect(response).to.have.property('name');
          expect(response).to.have.property('players');
          expect(response.players).to.be.an('array');
        });
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.body
      operator: isDefined

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
